package com.example.studyflow.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta do Tema Escuro (Dark Theme)
val DarkBackground = Color(0xFF120A2A)       // Fundo azul muito escuro, quase preto (#090914)
val DarkSurface = Color(0xFF151528)          // Fundo dos cartões e superfícies (#151528)
val DarkSurfaceVariant = Color(0xFF1E1E38)   // Fundo de entradas e elementos secundários (#1E1E38)

// Paleta do Tema Claro (Light Theme)
val LightBackground = Color(0xFFF6F6FA)      // Fundo claro suave (#F6F6FA)
val LightSurface = Color(0xFFFFFFFF)         // Superfícies de cartões e cards (#FFFFFF)
val LightSurfaceVariant = Color(0xFFEDEBF7)  // Elementos secundários/entradas de texto claras (#EDEBF7)

val PrimaryAccent = Color(0xFF7C4DFF)        // Roxo vibrante (#7C4DFF) para botões flutuantes, principais e itens ativos
val PrimaryAccentLight = Color(0xFF9E7CFF)   // Variação clara do roxo

val TextPrimary = Color(0xFFFFFFFF)          // Texto principal no tema escuro
val TextSecondary = Color(0xFF8B8B9E)        // Texto secundário cinza-azulado (#8B8B9E)
val TextOnPrimary = Color(0xFFFFFFFF)        // Texto sobre botões roxos

val LightTextPrimary = Color(0xFF1C1B20)     // Texto principal no tema claro
val LightTextSecondary = Color(0xFF6E6E82)   // Texto secundário no tema claro

val AlertError = Color(0xFFFF5252)           // Vermelho (#FF5252) para limite de faltas e alertas
val AlertErrorContainer = Color(0xFF3D1B28)  // Fundo de badge de erro
val AlertSuccess = Color(0xFF4ADE80)         // Verde para zero faltas e sucesso
val AlertWarning = Color(0xFFFFB74D)         // Laranja para alertas
